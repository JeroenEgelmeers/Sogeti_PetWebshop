'use strict';

describe('myApp.assortiment module', function() {

  beforeEach(module('myApp.assortiment'));

  describe('assortiment controller', function(){

    it('should ....', inject(function($controller) {
      //spec body
      var AssortimentController = $controller('AssortimentController');
      expect(AssortimentController).toBeDefined();
    }));

  });
});